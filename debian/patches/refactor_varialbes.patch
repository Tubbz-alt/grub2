Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 grub2 (2.02+dfsg1+deepin4-2deepin1) eagle; urgency=medium
 .
   * Add sbverify for arm64.
   * Change signature name to sig_%s.
Author: Zhang Yueqian <zhangyueqian@deepin.com>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2019-12-20

--- grub2-2.02+dfsg1+deepin4.orig/grub-core/loader/arm64/linux.c
+++ grub2-2.02+dfsg1+deepin4/grub-core/loader/arm64/linux.c
@@ -288,6 +288,9 @@ grub_cmd_linux (grub_command_t cmd __att
   grub_file_t file = 0;
   struct linux_armxx_kernel_header lh;
 
+    // for verify
+  char *verify_args[2] = { NULL, NULL };
+
   grub_dl_ref (my_mod);
 
   if (argc == 0)
@@ -319,11 +322,9 @@ grub_cmd_linux (grub_command_t cmd __att
       grub_dl_ref (mod);
       verify_detached_cmd = grub_command_find ("verify_detached");
 
-      fnlen = grub_strlen (argv[0]);
-
       verify_args[0] = argv[0];
 
-      signamelen = fnlen + 4;
+      signamelen = grub_strlen (argv[0]) + 4;
       verify_args[1] = grub_malloc (signamelen + 1); // filename + .sig
       grub_snprintf (verify_args[1], signamelen + 1, "sig_%s", argv[0]);
 
--- grub2-2.02+dfsg1+deepin4.orig/grub-core/loader/i386/linux.c
+++ grub2-2.02+dfsg1+deepin4/grub-core/loader/i386/linux.c
@@ -702,7 +702,6 @@ grub_cmd_linux (grub_command_t cmd __att
   grub_uint64_t preferred_address = GRUB_LINUX_BZIMAGE_ADDR;
 
   // for verify
-  int fnlen = grub_strlen(argv[0]);
   char *verify_args[2] = { NULL, NULL };
 
   grub_dl_ref (my_mod);
@@ -764,11 +763,9 @@ grub_cmd_linux (grub_command_t cmd __att
       grub_dl_ref (mod);
       verify_detached_cmd = grub_command_find ("verify_detached");
 
-      fnlen = grub_strlen (argv[0]);
-
       verify_args[0] = argv[0];
 
-      signamelen = fnlen + 4;
+      signamelen = grub_strlen (argv[0]) + 4;
       verify_args[1] = grub_malloc (signamelen + 1); // filename + .sig
       grub_snprintf (verify_args[1], signamelen + 1, "sig_%s", argv[0]);
 
